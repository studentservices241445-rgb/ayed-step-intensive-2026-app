import { randomInt, showToast } from './utils.js';

const messages = [
  '🔥 طالب من جدة سجّل قبل شوي — لا يفوتك العرض!',
  '✅ تم تأكيد دفعة جديدة — المقاعد تتحرك بسرعة.',
  '📌 تذكير: لو ما تعرف مستواك، سو اختبار الـ 20 سؤال داخل التسجيل.',
  '🎧 نصيحة سريعة: الاستماع في STEP يحتاج تدريب يومي حتى لو 10 دقايق.',
  '⏳ لا تنسى: السعر بيرجع للسعر القديم بعد انتهاء المؤقت.',
];

export function initToastsAuto() {
  if (!window.AYED_CONFIG?.ui?.enableToasts) return;

  function schedule() {
    const wait = randomInt(40, 95) * 1000;
    setTimeout(() => {
      showToast(messages[randomInt(0, messages.length - 1)], 'info');
      schedule();
    }, wait);
  }

  // أول إشعار بعد كم ثانية
  setTimeout(schedule, 8000);
}
